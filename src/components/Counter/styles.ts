import { animated } from '@react-spring/web'

import styled from 'styled-components'

export const Wrapper = styled(animated.h1)`
  font-size: 160px;
  color: #f5f5f5;
  position: absolute;
  right: 0;
  transform: rotate(90deg);
  z-index: -1;
`
